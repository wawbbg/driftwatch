// Package rollup aggregates per-service drift results into a single
// cross-service Report, providing counts, drift detection, and a
// formatted tabular summary suitable for CLI output or further processing.
package rollup
