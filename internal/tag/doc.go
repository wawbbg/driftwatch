// Package tag provides a lightweight tagging system for annotating drift
// results with arbitrary key=value metadata.
//
// Tags can be sourced from CLI flags, config files, or environment variables
// and are carried through reports and audit records to aid filtering and
// triage in downstream systems.
package tag
